query getCourse($id: ID!){
  course(id : $id) {
    id,
    code,
    type,
    title,
    address,
    city,
    country,
    description,
    price,
    discount_price,
    currency,
    start_date,
    end_date,
    max_attendees,
  }
}
